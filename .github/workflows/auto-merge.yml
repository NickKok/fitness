name: Auto-Merge Approved Routines
on:
  issue_comment:
    types: [created]
jobs:
  parse-and-update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Parse Issue
        if: contains(github.event.comment.body, 'APPROVE')
        run: |
          # (I’ll handle the parsing logic here)
          echo "PR created for ${{ github.event.issue.title }}"
      - name: Create PR
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "Add routine: ${{ github.event.issue.title }}"
          branch: "update-${{ github.event.issue.number }}"
